#!/usr/bin/env node

process.title = 'wow';

var path = require('path');

process.argv = [
    'node',
    'grunt',
    'local',
    '-b',
    '.',
    '--gruntfile',
    path.resolve(__dirname, '../lib') + path.sep + 'Gruntfile.js'
];

// init Grunt task
require('grunt').cli();